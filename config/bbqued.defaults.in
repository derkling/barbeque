# BarbequeRTRM Daemon Configuration Options
# Sourced by: sourced by ${CMAKE_INSTALL_PREFIX}/${BBQUE_PATH_INIT}/bbqued
#
# NOTE: if possible, use absolute paths
#
#
# The BBQ binary
BIN='${CMAKE_INSTALL_PREFIX}/${BBQUE_PATH_BBQ}/barbeque'
#
# The BBQ configuration file
CONF='${CMAKE_INSTALL_PREFIX}/${BBQUE_PATH_CONF}/${BBQUE_CONF_FILE}'
#
# The BBQ daemon PID file
PIDFILE='${CMAKE_INSTALL_PREFIX}/${BBQUE_DAEMON_PIDFILE}'
#
# The BBQ daemon lock file
LOCKFILE='${CMAKE_INSTALL_PREFIX}/${BBQUE_DAEMON_LOCKFILE}'
#
# The folder containing PIL Layouts
PILSETUPS='${CMAKE_INSTALL_PREFIX}/${BBQUE_PATH_PILS}'
#
# The PIL Layout to use (if an host specific one is not available)
PILLAYOUT='default'
#
# The PIL Layout parser (distributed with BarbequeRTRM)
PILPARSER='${CMAKE_INSTALL_PREFIX}/sbin/bbque-plp'
#
# The folder containing (compiled) CGroups configuration
CGCONFS='${CMAKE_INSTALL_PREFIX}/${BBQUE_PATH_VAR}'
#
# The CGroup configuration parser (part of libcgroup library)
CGPARSER='${CMAKE_INSTALL_PREFIX}/sbin/cgconfigparser'
#
# The mountpoint of the BBQ CGroup hierarchy
CGMOUNT='${CMAKE_INSTALL_PREFIX}/${BBQUE_PIL_CGROUP_MOUNT}'
#
# vim: set tabstop=4:

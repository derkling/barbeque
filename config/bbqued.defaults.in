# BarbequeRTRM Daemon Configuration Options
# Sourced by: sourced by ${CONFIG_BOSP_RUNTIME_PATH}/${BBQUE_PATH_INIT}/bbqued
#
# NOTE: if possible, use absolute paths
#
#
# The BBQ binary
BIN='${CONFIG_BOSP_RUNTIME_PATH}/${BBQUE_PATH_BBQ}/barbeque'
#
# The BBQ configuration file
CONF='${CONFIG_BOSP_RUNTIME_PATH}/${BBQUE_PATH_CONF}/${BBQUE_CONF_FILE}'
#
# The BBQ run-time folder
BRTF='${CMAKE_INSTALL_PREFIX}/${BBQUE_PATH_VAR}'
#
# The BBQ daemon PID file
PIDFILE='${CONFIG_BOSP_RUNTIME_PATH}/${BBQUE_DAEMON_PIDFILE}'
#
# The BBQ daemon lock file
LOCKFILE='${CONFIG_BOSP_RUNTIME_PATH}/${BBQUE_DAEMON_LOCKFILE}'
#
# The BBQ daemon (default) LOG file
LOGFILE='${CONFIG_BOSP_RUNTIME_PATH}/${BBQUE_DAEMON_LOGFILE}'
#
# The folder containing PIL Layouts
PILSETUPS='${CONFIG_BOSP_RUNTIME_PATH}/${BBQUE_PATH_PILS}'
#
# The PIL Layout to use (if an host specific one is not available)
PILLAYOUT='default'
#
# The PIL Layout parser (distributed with BarbequeRTRM)
PILPARSER='${CONFIG_BOSP_RUNTIME_PATH}/sbin/bbque-plp'
#
# The folder containing (compiled) CGroups configuration
CGCONFS='${CONFIG_BOSP_RUNTIME_PATH}/${BBQUE_PATH_VAR}'
#
# The CGroup configuration parser (part of libcgroup library)
CGPARSER='${CONFIG_BOSP_RUNTIME_PATH}/sbin/cgconfigparser'
#
# The mountpoint of the BBQ CGroup hierarchy
CGMOUNT='${CONFIG_BOSP_RUNTIME_PATH}/${BBQUE_PIL_CGROUP_MOUNT}'
#
# The prefix for the BBQ installation
SYSROOT='${CONFIG_BOSP_RUNTIME_PATH}'
#
# vim: set tabstop=4:
